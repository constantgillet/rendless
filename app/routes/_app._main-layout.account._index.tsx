import * as m from "~/paraglide/messages";

export const handle = {
  pageTitle: m.settings(),
};

export default function AccountPage() {
  return (
    <div>
      <h1>Account</h1>
    </div>
  );
}
